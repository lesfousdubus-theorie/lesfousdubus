---
interface Props {
  icon: string;
  title: string;
  description: string;
  href: string;
  count?: number;
}

const { icon, title, description, href, count } = Astro.props;
---

<a href={href} class="card card-hover">
  <div class="card__header">
    <span class="card__icon" aria-hidden="true">{icon}</span>
    {count !== undefined && (
      <span class="card__count">{count} article{count > 1 ? 's' : ''}</span>
    )}
  </div>
  <h3 class="card__title">{title}</h3>
  <p class="card__desc">{description}</p>
  <span class="card__arrow" aria-hidden="true">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
  </span>
</a>

<style>
  .card {
    position: relative;
    display: flex;
    flex-direction: column;
    padding: 20px;
    background: var(--gradient-card);
    border: 1px solid var(--border-color);
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    text-decoration: none;
    color: var(--text-main);
    transition:
      border-color 0.25s ease,
      box-shadow 0.25s ease,
      transform 0.25s ease;
    isolation: isolate;
  }

  .card:focus-visible {
    outline: 2px solid var(--cyan);
    outline-offset: 3px;
  }

  .card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    padding: 1px;
    background: var(--gradient-border);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude;
    -webkit-mask-composite: xor;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .card:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 36px rgba(0,0,0,0.20), 0 0 22px var(--glow-violet);
    border-color: var(--violet);
  }

  .card:hover::before {
    opacity: 1;
  }

  .card:active {
    transform: translateY(0) scale(0.98);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15), 0 0 10px var(--glow-violet);
  }

  .card__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
  }

  .card__icon {
    font-size: 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--border-radius);
    background: color-mix(in srgb, var(--violet) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--violet) 15%, transparent);
    transition: all 0.3s ease;
  }

  .card:hover .card__icon {
    background: color-mix(in srgb, var(--violet) 18%, transparent);
    border-color: color-mix(in srgb, var(--violet) 30%, transparent);
    box-shadow: 0 0 12px var(--glow-violet);
  }

  .card__count {
    font-size: 11.5px;
    color: var(--text-secondary);
    background-color: var(--surface);
    padding: 3px 8px;
    border-radius: var(--border-radius-full);
    font-weight: 500;
  }

  .card__title {
    font-size: 16px;
    font-weight: 600;
    color: var(--text-main);
    margin-bottom: 6px;
    transition: color 0.25s ease;
  }

  .card:hover .card__title {
    color: var(--cyan);
  }

  .card__desc {
    font-size: 13.5px;
    color: var(--text-secondary);
    line-height: 1.55;
    flex: 1;
  }

  .card__arrow {
    position: absolute;
    bottom: 16px;
    right: 16px;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--border-radius-full);
    background: transparent;
    color: var(--text-secondary);
    opacity: 0;
    transform: translateX(-4px);
    transition: all 0.25s ease;
  }

  .card:hover .card__arrow {
    opacity: 1;
    transform: translateX(0);
    color: var(--cyan);
  }
</style>
